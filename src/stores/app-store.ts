/**
 * @deprecated Import `useAppStore` from `../store/appStore`.
 *
 * Temporary compatibility shim while frontend files migrate to one store
 * directory. The state itself lives only in `src/store/appStore.ts`.
 */
export { useAppStore } from '../store/appStore'
