name: v0.3.0 Version Align

on:
  push:
    branches:
      - dev/v0.3.0-rule-model-v2
    paths:
      - '.github/workflows/v030-version-align.yml'

permissions:
  contents: write

jobs:
  align:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: dev/v0.3.0-rule-model-v2
      - name: Align package versions
        shell: python
        run: |
          from pathlib import Path
          replacements = {
              'package.json': [('"version": "0.2.4"', '"version": "0.3.0"')],
              'src-tauri/tauri.conf.json': [('"version": "0.2.4"', '"version": "0.3.0"')],
              'src-tauri/Cargo.toml': [('version = "0.2.4"', 'version = "0.3.0"')],
              'src-tauri/Cargo.lock': [('name = "keymaster-pro"\nversion = "0.2.4"', 'name = "keymaster-pro"\nversion = "0.3.0"')],
          }
          for filename, pairs in replacements.items():
              path = Path(filename)
              text = path.read_text(encoding='utf-8')
              for old, new in pairs:
                  if old not in text:
                      raise SystemExit(f'expected version marker not found in {filename}: {old}')
                  text = text.replace(old, new, 1)
              path.write_text(text, encoding='utf-8')
      - name: Commit aligned versions
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add package.json src-tauri/tauri.conf.json src-tauri/Cargo.toml src-tauri/Cargo.lock
          git commit -m 'chore(0.3.0): align application version'
          git push origin HEAD:dev/v0.3.0-rule-model-v2
